# Prompt与交互记录

本目录记录各阶段实际遵循的任务指令、输入边界和结构化输出约束。

模型与交互的可核验索引位于 `model_interaction_manifest.json`，
对应的结构化输出保存在 `logs/model_interactions/structured_outputs/`。
未在本地运行记录中形成的模型参数字段标记为 `UNKNOWN_NOT_RECORDED`，不补造。
